name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - main

# Cloudflare Pages is connected directly to this GitHub repo and deploys
# automatically on every push to main. No GitHub Action is needed here.
# This workflow is intentionally left with no jobs.
jobs: {}
