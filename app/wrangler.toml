name = "head-hub"
compatibility_date = "2024-01-01"

[env.production]
name = "head-hub"

[env.preview]
name = "head-hub-preview"

# Configuración para Next.js
[build]
command = "npm run build"
cwd = "."

# Variables de entorno (se configurarán en el dashboard)
[vars]
NODE_ENV = "production"

